{products.map(({ id, title, price, rating, thumbnail }: any) => {
            return (
              // <>
              <List
              className="List"
              height={150}
              itemCount={itemCount}
              itemSize={30}
              onItemsRendered={onItemsRendered}
              ref={ref}
              width={300}
            >
              {Item}
            </List>
              // </>
              // <Box key={id} display={"flex"} flexDirection={"column"}>
              //   <Typography variant="h6">{title}</Typography>
              //   <Image src={thumbnail} alt={"asd"} width={100} height={100} />
                {/* <Typography variant="p">{rating}</Typography>
              <Typography variant="p">{price}</Typography> */}
              {/* </Box> */}
            );
          })}